#' Vectorized version of function <%=fun_name%>
#'
#' This is the vectorized version of \code{\link{<%=fun_name%>}}.
#'
#' @param x List, where each element is an input to \code{x} in \code{\link{<%=fun_name%>}}.
#' @param y List, where each element is an input to \code{y} in \code{\link{<%=fun_name%>}}.
#' @param args \code{NULL} (default) or list where each element is an input to \code{args}
#' in \code{\link{<%=fun_name%>}}.
#'
#' @return List, where each element is an output from \code{\link{<%=fun_name%>}}.
#'
#' @export
#' @seealso \code{\link{<%=fun_name%>}} for the non-vectorized version.
